# Workspace runtime state

OpenClaw and workspace tools can create local state here. The real contents are generated and may contain paths, identifiers, prompts, messages, and lifecycle metadata.

Only explicitly named fictional examples are published.
