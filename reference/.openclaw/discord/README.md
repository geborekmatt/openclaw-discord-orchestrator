# Discord runtime state

OpenClaw stores generated Discord command-deployment state here. Bot tokens, account IDs, guild IDs, user IDs, and runtime caches are excluded.

Public Discord configuration is documented in [`../openclaw.example.json5`](../openclaw.example.json5). The bot token is represented by the `DISCORD_BOT_TOKEN` environment variable, and all Discord IDs use obvious placeholders.
